# 基本信息

技术/论文标题：Symbolon: Learning Transformations for Symbolic Execution

作者/团队：Jie Zhu、Penghui Li、Zhongxuan Li、Chihao Shen、Ziyang Li、Yizheng Chen、Kexin Pei。来自芝加哥大学 CIRRUS Lab，实验室方向是代码智能与可信安全软件系统，另有 ExploitGym、Locus 等安全评测项目。Kexin Pei 在符号执行与程序分析领域有较高知名度

发表刊物/来源：arXiv

发布时间：2026年6月

地址：https://arxiv.org/abs/2606.29108v2  https://github.com/cirrus-uchicago/Symbolon

类型：技术趋势、Paper、开源项目

标签：符号执行、LLM Agent、代码转换、KLEE、覆盖率优化

技术成熟度：研究阶段，论文配套开源仓库，含 Nix 构建环境和完整评估脚本

# 一句话总结

用 LLM 驱动的 agent 自动学习多样化的代码转换技能，上下文敏感地应用到目标程序源码上，通过转换并重放奖励机制确保转换探索与原始程序的符号执行目标一致，最终提升 KLEE 符号执行覆盖率。

# 关键结论

核心结论：在 32 个真实世界程序上使 KLEE 覆盖率平均提升 3.69 倍，峰值内存降低 29.2 倍，单次求解器时间减少 123 倍。应用于最新 Linux 内核发现 21 个此前未知漏洞。核心贡献是把转换发现公式化为程序表示空间上的搜索问题，用转换并重放奖励机制确保转换后的程序只充当测试生成的脚手架，奖励信号始终来自原始程序。

适用场景：符号执行覆盖率瓶颈突破，测试用例生成优化，符号执行路径爆炸缓解。

不适用场景：不需要符号执行的项目，非 C/C++ 程序的覆盖率优化，运行环境无法安装 KLEE 和 Nix 的场景。

是否值得关注：值得。把 LLM agent 引入符号执行优化是一个新颖且实用的方向，实际发现了 Linux 内核的未知漏洞。

# 为什么值得关注

背景与问题：符号执行在真实程序上长期受困于路径爆炸、复杂约束求解等可扩展性瓶颈。传统方法依赖编译器优化或手工编写的语义保持转换，但这些转换过于刚性，无法利用多样化的局部程序语义。每个程序的最优转换可能不同，手工枚举所有可能的转换不现实。

现有方案存在什么不足：编译器优化是通用的，不对特定程序做适配。手工编写的转换规则数量有限，覆盖的场景有限。自动转换发现方法缺乏可靠的奖励信号，无法保证转换确实提升了原始程序的符号执行效果。

为什么现在值得关注：LLM 的代码理解能力已经到了可以自动发现和生成程序特定转换的程度。Symbolon 把转换发现变成搜索问题，用 LLM 在小规模程序上低成本探索，蒸馏成可复用的技能库，再上下文敏感地应用到目标程序上。转换并重放奖励机制保证奖励信号始终来自原始程序，不是转换后的程序，这个设计很关键。

# 技术细节

核心思想：三段式。离线学习阶段用 LLM 在小规模程序上探索转换，蒸馏为可复用的技能库，每个技能记录覆盖率增量和来源信息。在线推理阶段 LLM agent 根据目标代码的局部上下文从技能库动态选择并实例化合适的转换技能。转换并重放奖励阶段，转换后的程序只充当测试生成的脚手架，KLEE 在转换后的程序上生成测试用例，然后重放到原始程序的覆盖率 oracle 上，以原始程序上的覆盖率提升作为奖励信号。

## 转换并重放奖励机制

这是论文最核心的贡献。双轨构建是基础：bitcode 轨用 gclang 编译带 KLEE_BITCODE 宏加 KLEE include 路径，产出 program.bc 供 KLEE 符号执行；cov 轨用 LLVM 16 clang 编译带覆盖率插桩，不加 KLEE_BITCODE 宏，产出 program.cov 作为覆盖率 oracle。KLEE_BITCODE 宏是双轨分离的开关，确保覆盖率 oracle 是原始程序的字节级不变副本。KLEE 在转换后的程序上生成 .ktest 测试用例，然后 klee-replay-all 把每个测试用例重放到覆盖率 oracle 上生成 .profraw，cov-merge 合并后由 llvm-cov 提取 lines-covered 作为奖励信号。覆盖率始终在原始未转换的程序上测量，转换后的程序只负责生成测试用例。

## Agent Loop 编排者代理分离

agent loop 采用编排者代理分离架构。编排者拥有测量循环和沙箱，代理只负责编辑源码。闭环流程是：编排者对转换后的程序运行 KLEE，把运行统计和覆盖率报告消化为反馈文件，代理读取反馈加技能库后编辑源码，编排者重建并重跑 KLEE，直到覆盖率平台期或预算耗尽。代理不能自己运行 KLEE，奖励信号完全由编排者测量后反馈给代理，这保证了奖励信号的客观性。

沙箱设计通过 PreToolUse hook 实现默认拒绝策略，代理只能读工作源码树加技能目录加 KLEE 输出目录，只能写工作源码树。Bash、WebFetch、WebSearch 全部禁用。技能目录只读，确保代理不污染技能库。

与现有方案的区别：传统方法用固定转换规则，Symbolon 用 LLM 自动发现转换。传统方法的奖励信号可能来自转换后的程序，Symbolon 的奖励始终来自原始程序。

# 验证与证据

实验设置：在 32 个真实世界程序上评测，agent loop 全自动运行，编排者测量覆盖率反馈给代理。论文正文标注了代码发布链接，仓库包含完整评估脚本和 Nix 构建环境。

数据集：32 个真实世界程序，以及 Linux 内核模块的实际应用。

核心指标：KLEE 覆盖率提升倍数，峰值内存降低倍数，单次求解器时间减少倍数，发现未知漏洞数量。

主要结果：覆盖率平均提升 3.69 倍，峰值内存降低 29.2 倍，求解器时间减少 123 倍。在 Linux 内核上发现 21 个此前未知漏洞。

作者结论：LLM 驱动的转换发现加转换并重放奖励可以有效提升符号执行覆盖率，且发现的真实漏洞证明了实际价值。

# 复现情况

是否复现：部分复现。代码可跑，但 32 个程序的完整实验未机检复现。

复现环境：依赖 Nix flake 构建环境、KLEE 的 feat/symbolon 分支、LLVM 16、gclang、Coccinelle 语义补丁工具、Claude Agent SDK。构建链复杂。

复现步骤：

```
cd containers
nix develop
cd scripts
eval-symbolon --program <name>
```

复现结果：入口脚本 eval-symbolon 可以启动，内部先复制源码到工作目录构建双轨 bitcode，运行 baseline KLEE 获取基线覆盖率，然后进入 agent loop。完整 32 程序评估未在保留容器中实跑，因为 codex SDK 看门狗中断。

复现成本：高。Nix 加 KLEE 特殊分支加 LLVM 16 加 gclang 加 Coccinelle 的构建链门槛很高，在线推理阶段还需要 Claude Agent SDK 的 API 费用。

# 局限与风险

技术局限：运行环境门槛高，依赖 Nix 加 KLEE 特殊分支加 LLVM 16 加 gclang 加 Coccinelle，构建链复杂。在线推理阶段需要 Claude Agent SDK，有 API 成本。

实验局限：codex SDK 看门狗中断导致实验复现降级，32 个程序的完整评估未机检复现。summary.json 中记录的 cost_usd 表明完整评估有不可忽略的 API 费用。

适用边界：仅适用于 C/C++ 程序的符号执行优化，且依赖 KLEE 引擎。其他语言的符号执行需要重新构建双轨基础设施。

工程风险：KLEE 的 feat/symbolon 分支是定制版本，如果上游 KLEE 不合并这些改动，长期维护成本高。Coccinelle 语义补丁的匹配模式可能对某些代码结构不适用。

# 个人思考

## 可借鉴性

转换并重放奖励机制是这套系统里最有价值的部分。双轨构建加 KLEE 在转换后程序上生成测试用例加重放到覆盖率 oracle 上测量奖励，这个设计解决了转换可以破坏语义但奖励信号必须来自原始程序的核心矛盾。任何需要在符号执行中评估源码转换效果的工作都可以直接复用这套流水线设计。

约束钉住技术也值得拿过来用。用 klee_range 创建二值符号选择器，在匹配分支用逐字节钉住符号缓冲区等于固定字面量，把 O(N) 的路径分叉压缩为 O(1) 完成。任何使用 KLEE 做符号执行的项目遇到字符串比较导致的路径爆炸时可以直接移植。

state-killer-finder 静态分析器可以在把程序交给符号执行引擎之前先扫描定位会导致状态终止的代码点，这个前置诊断思路适合任何符号执行工具链。

可以应用到哪些现有项目：符号执行工具链优化、模糊测试与符号执行混合引擎、代码质量分析工具。

后续是否跟进：会关注。主要看 KLEE 特殊分支是否会合并到上游，以及这套方法能否扩展到非 C/C++ 语言的符号执行。

下一步行动：评估转换并重放奖励机制能否移植到团队内部的测试用例生成流水线中，另外看看约束钉住技术能否直接用在我们现有的 KLEE 任务上。